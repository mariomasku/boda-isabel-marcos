---
import Layout from '../layouts/Layout.astro';
import Hero from '../components/Hero.astro';
import Welcome from '../components/Welcome.astro';
import Details from '../components/Details.astro';
import Bus from '../components/Bus.astro';
import RSVP from '../components/RSVP.astro';
import Footer from '../components/Footer.astro';
import PhotoParallax from '../components/PhotoParallax.astro';
import '../styles/global.css';
---

<Layout>
  <nav id="navbar" class="navbar">
    <div class="nav-names"><span class="dash-logo">Isabel <span style="font-style:normal;color:color-mix(in srgb, var(--oro) 50%, transparent)">&</span> Marcos</span></div>
    <div class="nav-links">
      <a href="#detalles">El día</a>
      <a href="#autobus">Bús</a>
      <a href="#rsvp">Confirmar</a>
    </div>
  </nav>

  <button type="button" id="darkside-toggle" class="darkside-toggle-btn">✦ El lado Jedi</button>

  <main>
    <Hero />
    <Welcome />
    <Details />
    <PhotoParallax foto="/fotos/foto2.webp" height="60vh" />
    <Bus />
    <PhotoParallax foto="/fotos/foto3.webp" height="60vh" />
    <RSVP />
    <PhotoParallax foto="/fotos/foto4.webp" height="50vh" />
    <Footer />
  </main>
</Layout>

<style>
  .navbar {
    position: fixed;
    top: 0;
    left: 0;
    right: 0;
    z-index: 100;
    display: flex;
    align-items: center;
    justify-content: space-between;
    padding: 1.2rem 2rem;
    transition: background 0.4s ease, backdrop-filter 0.4s ease, box-shadow 0.4s ease;
  }

  .navbar.scrolled {
    background: color-mix(in srgb, var(--sombra) 92%, transparent);
    backdrop-filter: blur(12px);
    box-shadow: 0 1px 0 color-mix(in srgb, var(--oro) 15%, transparent);
  }

  .nav-names {
    font-family: 'Playfair Display', serif;
    font-size: 1rem;
    color: var(--oro);
    letter-spacing: 0;
    font-style: italic;
  }

  .nav-links {
    display: flex;
    gap: 2rem;
  }

  .nav-links a {
    color: color-mix(in srgb, var(--marfil) 80%, transparent);
    text-decoration: none;
    font-family: 'Lato', sans-serif;
    font-size: 0.78rem;
    letter-spacing: 0.2em;
    text-transform: uppercase;
    transition: color 0.2s;
  }

  .nav-links a:hover { color: var(--oro); }

  @media (max-width: 480px) {
    .nav-links { gap: 1rem; }
    .nav-links a { font-size: 0.7rem; }
  }

  .darkside-toggle-btn {
    position: fixed;
    bottom: 1.5rem;
    right: 1.5rem;
    z-index: 100;
    display: flex;
    align-items: center;
    gap: 0.5rem;
    padding: 0.7rem 1.3rem;
    border-radius: 24px;
    background: color-mix(in srgb, var(--eucalipto-noche) 88%, transparent);
    backdrop-filter: blur(6px);
    -webkit-backdrop-filter: blur(6px);
    border: 1px solid color-mix(in srgb, var(--oro) 45%, transparent);
    color: var(--oro);
    font-family: 'Lato', sans-serif;
    font-size: 0.72rem;
    letter-spacing: 0.12em;
    text-transform: uppercase;
    cursor: pointer;
    transition: background 0.2s, color 0.2s, transform 0.15s;
  }

  .darkside-toggle-btn:hover { background: color-mix(in srgb, var(--oro) 16%, transparent); color: var(--marfil); }
  .darkside-toggle-btn:active { transform: scale(0.95); }

  @media (max-width: 480px) {
    .darkside-toggle-btn { bottom: 1rem; right: 1rem; padding: 0.6rem 1rem; font-size: 0.65rem; }
  }
</style>

<script>
  import { gsap } from 'gsap';
  import { ScrollTrigger } from 'gsap/ScrollTrigger';

  gsap.registerPlugin(ScrollTrigger);

  // Navbar scroll effect
  const navbar = document.getElementById('navbar')!;
  window.addEventListener('scroll', () => {
    navbar.classList.toggle('scrolled', window.scrollY > 60);
  });

  // Hero: entrada escalonada del texto
  gsap.fromTo('.gsap-hero',
    { opacity: 0, y: 40 },
    {
      opacity: 1,
      y: 0,
      duration: 1,
      stagger: 0.18,
      ease: 'power3.out',
      delay: 0.3,
    }
  );

  // Hero: parallax del fondo al hacer scroll
  gsap.to('.parallax-hero-bg', {
    yPercent: 25,
    ease: 'none',
    scrollTrigger: {
      trigger: '#hero',
      start: 'top top',
      end: 'bottom top',
      scrub: true,
    },
  });

  // Parallax en secciones de foto + zoom suave de imagen
  gsap.utils.toArray<HTMLElement>('.parallax-section').forEach((section) => {
    const bg = section.querySelector<HTMLElement>('.parallax-bg');
    if (!bg) return;
    gsap.fromTo(bg,
      { yPercent: -15, scale: 1.0 },
      {
        yPercent: 15,
        scale: 1.3,
        ease: 'none',
        scrollTrigger: {
          trigger: section,
          start: 'top bottom',
          end: 'bottom top',
          scrub: true,
        },
      }
    );
  });

  // Fade-in en scroll para elementos de contenido
  gsap.utils.toArray<HTMLElement>('.gsap-fade').forEach((el) => {
    gsap.fromTo(el,
      { opacity: 0, y: 35 },
      {
        scrollTrigger: {
          trigger: el,
          start: 'top 88%',
          toggleActions: 'play none none none',
        },
        opacity: 1,
        y: 0,
        duration: 0.85,
        ease: 'power2.out',
      }
    );
  });

  // Botón para cambiar al diseño Star Wars, con confirmación previa
  document.getElementById('darkside-toggle')?.addEventListener('click', () => {
    if (window.confirm('¿Quieres ver la invitación de otra galaxia?')) {
      window.location.href = '/darkside';
    }
  });

  // Smooth scroll
  document.querySelectorAll('a[href^="#"]').forEach((anchor) => {
    anchor.addEventListener('click', (e) => {
      e.preventDefault();
      const target = document.querySelector((anchor as HTMLAnchorElement).getAttribute('href')!);
      target?.scrollIntoView({ behavior: 'smooth', block: 'start' });
    });
  });
</script>
